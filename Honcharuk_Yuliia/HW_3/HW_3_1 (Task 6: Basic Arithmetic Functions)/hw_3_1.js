const a = 7;
const b = 15;

function sum (a,b){
    return (a + b)
}

function multiplication(a,b){
    return (a * b)
}

console.log(`The sum of ${a} and ${b} is ${sum(a,b)}.`);
console.log(`The product of ${a} and ${b} is ${multiplication(a,b)}.`)